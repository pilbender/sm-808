Simple implementation of the sm-808 drum machine.

Dependencies:
 - Java 8
 - Maven 3.x or higher

To build:
mvn clean install

To run tests:
mvn test

To run (artifact is in the "target" directory):
java -jar sm-808-1.0-SNAPSHOT.jar